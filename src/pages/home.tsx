import { Button } from "@chakra-ui/react";

export const HomePage = () => {
    return (
        <div>
            <h1>Home</h1>
            <Button colorScheme="brand">Click me</Button>
        </div>
    );
};
